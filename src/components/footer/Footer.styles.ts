import { createStyles } from "@material-ui/core";

const styles = () =>
  createStyles({
    footer: {
      position: "absolute",
      left: 0,
      bottom: 0,
      right: 0,
      padding: 20,
      textAlign: "center"
    }
  });

export default styles;
